import React from 'react';

const MainContainer = (props) => {
    // all logic like state, onChange etc...

    return (
        <div>
          <Search {} />
          <Result />
        </div>
    );
}

export default MainContainer;